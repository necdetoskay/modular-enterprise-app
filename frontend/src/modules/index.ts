/**
 * Module Registry
 * 
 * This file serves as a registry for all modules in the application.
 * When adding a new module, register it here to make it available to the application.
 */

// Import module components, hooks, and types
// Example: import { VehicleList, VehicleForm } from './vehicles/components';

// Module components object
const moduleComponents = {
  // Example: VehicleList, VehicleForm
};

// Module hooks object
const moduleHooks = {
  // Example: useVehicles, useVehicleForm
};

// Module types object
const moduleTypes = {
  // Example: Vehicle, VehicleStatus
};

export {
  moduleComponents,
  moduleHooks,
  moduleTypes
};
